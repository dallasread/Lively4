import {
  truncateText
} from 'lively/helpers/truncate-text';

module('TruncateTextHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = truncateText(42);
  ok(result);
});
