<div class="pane">
	
	<input type="text" class="visitors_search" placeholder="Search...">

	<div class="visitors">
		{{#each}}
			{{#link-to 'admin.visitors.visitor' . classBinding=':visitor online:online agent_has_unread:has_unread'}}
				<span class="name">
					<span {{bind-attr title=details.email}}>{{ details.name }}</span>
				</span>
				<span class="icons">
					<i title="Apple" class="ion ion-social-apple"></i>
					<i title="Windows" class="ion ion-social-windows"></i>
					<i title="Canada" class="ion ion-flag"></i>
				</span>
				<span class="last_seen">
					{{ago visitor_last_seen}}
				</span>
				<span class="clear"></span>
			{{/link-to}}
		{{/each}}
		
	</div>
	
	{{outlet}}
	
</div>