<div>
	<form {{action "saveChatbox" on='submit'}}>

		<!-- <div class="field">
			<label>Organization Name</label>
			{{!input value=session.chatbox.name}}
		</div> -->
	
		<!-- <div class="field">
			<label>Which pages should <strong>show</strong> your chatbox?</label>
			{{!input value=session.chatbox.include}}
		</div>

		<div class="field">
			<label>Which pages should <strong>hide</strong> your chatbox?</label>
			{{!input value=session.chatbox.exclude}}
		</div> -->
		
		<div class="field">
			<label>Which colour should the chatbox header be (eg. red, green, #333333)?</label>
			{{input value=session.chatbox.color}}
		</div>
		
		<div class="field">
			<label>What message should be sent when no agents are available?</label>
			{{input value=session.chatbox.initial_offline_message}}
		</div>
		
		<div class="field">
			<label>What message should be sent when no agents are available?</label>
			{{input value=session.chatbox.initial_online_message}}
		</div>

		<button type="submit" class="full_width">Save Settings</button>
	</form>
</div>